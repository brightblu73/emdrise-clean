# === EMDRise environment template (safe to commit) ===
# Frontend (Vite exposes these)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_STRIPE_PUBLIC_KEY=

# Server-only (do NOT commit real values)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID=
SUPABASE_SERVICE_ROLE_KEY=

# Optional if your frontend talks to a separate backend host
API_BASE_URL=