# === EMDRise env template (copy these names into any host's env UI) ===
# Frontend (exposed to browser via Vite)
VITE_SUPABASE_URL=<paste your Supabase URL>
VITE_SUPABASE_ANON_KEY=<paste your Supabase anon key>
VITE_STRIPE_PUBLIC_KEY=<paste your Stripe publishable key>

# Server-only (KEEP SECRET)
STRIPE_SECRET_KEY=<paste your Stripe secret key>
STRIPE_WEBHOOK_SECRET=<paste your Stripe webhook secret>
STRIPE_PRICE_ID=<paste your Stripe price id>
SUPABASE_SERVICE_ROLE_KEY=<paste your Supabase service role key>

# Optional if frontend calls a separate backend host
API_BASE_URL=<https://your-backend-host-or-leave-blank>